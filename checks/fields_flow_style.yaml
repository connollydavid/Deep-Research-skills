# Regression fixture: flow-style field entries. Must parse to the exact
# same field set / required split as fields_block_style.yaml.
fields:
  basic_info:
    - {name: alpha, description: First field., detail_level: brief}
    - {name: beta, description: Second field, explicitly required., required: true}
  metrics:
    - {name: gamma, description: Third field., detail_level: brief}
    - {name: delta, description: Fourth field, explicitly optional., required: false}
uncertain: []
